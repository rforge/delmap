export(
       CleanData, 
       IdentifyMarkerBlocks,
       ImputeMissingGeno,
       SimDeletions,
       CreateDistMatrix,
       IdentifyMarkerOrd,
       PermuteCols,   
       ReadData
        )
S3method(print, delmap.data)
S3method(plot, delmap.data)
import(seriation, TSP)

